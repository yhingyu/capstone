# Step 8 demo media

The final GIF or screencast will be recorded after interactive acceptance testing.

Recommended 2–3 minute sequence:

1. Run `docker compose up --build`.
2. Open the dashboard and point out the POC/responsible-use notice.
3. Load the included sample data and generate a forecast.
4. Upload `data/sample/daily_readings_example.csv`.
5. Add or update one manual reading.
6. Explain the prediction band, advisory threshold, and estimated cost.
7. Show `/health`, the local monitoring log, and the passing GitHub Actions check.
8. State that AWS/cloud deployment is recommended future work, not part of this POC.

Save the final media here as `step8_dashboard_demo.gif` or `step8_dashboard_demo.mp4`.
